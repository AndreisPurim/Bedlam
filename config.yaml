general:
  debug: true
  log_dir: "logs"
  server_addr: "localhost:50051"
  notify_mode: "type"        # [type|untyped|poll]
  m2_mode: "blind"           # [blind|designated]
  poll_interval: 1.0
  random_jitter: true

clients:
  - id: 1
    role: "M1M3"
    epochs: 2
    batch_size: 64
  - id: 2
    role: "M1M3"
    epochs: 2
    batch_size: 64
  - id: 3
    role: "M2"
